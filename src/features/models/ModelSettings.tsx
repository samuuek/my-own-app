import { CheckCircle2, ExternalLink, KeyRound, Server, ShieldCheck, X } from 'lucide-react'
import type { ModelInfo } from './model-api'

export function ModelSettings({ models, onClose }: { models: ModelInfo[]; onClose: () => void }) {
  const webModel = models.find(model => model.kind === 'web-handoff')
  const apiModels = models.filter(model => model.kind === 'api')
  return <div className="settings-backdrop" role="presentation" onMouseDown={event => { if (event.target === event.currentTarget) onClose() }}><section className="model-settings" role="dialog" aria-modal="true" aria-labelledby="model-settings-title"><header><div><span className="eyebrow">选择反馈方式</span><h2 id="model-settings-title">对话服务</h2></div><button aria-label="关闭对话服务" onClick={onClose}><X/></button></header><p className="settings-intro">免费网页版不需要 API Key，也可以继续使用本地配置的 API 模型。</p>{webModel && <article className="web-service-card"><div className="provider-icon"><ExternalLink size={18}/></div><div><h3>DeepSeek 免费网页版</h3><p>登录自己的账号即可免费使用</p></div><span className="ready"><CheckCircle2 size={14}/>网页版可用</span></article>}<p className="privacy-note"><ShieldCheck size={16}/>思屿不会获取 DeepSeek 账号、密码或登录状态。</p><details className="api-settings"><summary>API 高级配置</summary><div className="provider-list">{apiModels.map(model => <article key={model.id}><div className="provider-icon"><Server size={18}/></div><div><h3>{model.provider}</h3><p>{model.name}</p></div><span className={model.available ? 'ready' : 'pending'}>{model.available ? <><CheckCircle2 size={14}/>已连接</> : <><KeyRound size={14}/>待配置</>}</span></article>)}</div><div className="settings-help"><b>配置方法</b><p>复制项目中的 <code>.env.example</code> 为 <code>.env.local</code>，填入对应 API Key、Base URL 和模型名称，然后重新启动网站。</p></div></details></section></div>
}
